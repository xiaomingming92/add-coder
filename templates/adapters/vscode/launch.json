{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-node",
            "request": "launch",
            "name": "Launch MCP Server",
            "program": "${workspaceFolder}/.vscode/scripts/mcp-server.ts",
            "preLaunchTask": "TS Node: Launch MCP Server",
            "cwd": "${workspaceFolder}",
            "env": {
                "NODE_ENV": "development"
            }
        }
    ]
}