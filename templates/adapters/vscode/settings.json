{
    "mcp": {
        "servers": {
            "{{projectName}}-dev-tools": {
                "command": "{{mcpServerCommand}}",
                "args": [
                    ".vscode/scripts/mcp-server.ts"
                ],
                "env": {
                    "NODE_ENV": "development"
                }
            }
        }
    }
}